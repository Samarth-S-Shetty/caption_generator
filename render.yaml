services:
  - type: web
    name: caption-generator
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
```

And add `ffmpeg-python` to `requirements.txt`:
```
flask
requests
python-dotenv
ffmpeg-python